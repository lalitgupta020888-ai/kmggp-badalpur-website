# Build output and dependencies — Railway installs and builds these itself
node_modules
.next
out
build

# Local admin content; production keeps this on the mounted volume instead
.data

# Source archives, not served by the app (public/ holds the served copies)
assets
PDF

# Local and other-platform config
.git
.vercel
.env*
*.log
